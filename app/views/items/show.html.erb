<div class="container">
  <div class="row"></div>
  <div class="col-sm-3">
    <h5><%= @item.name %></h5>
    <%= image_tag(@item.image_url) %>
    <p>
      <%= @item.description %>
    </p>
    <%= button_to "Add Order", {action: "create", controller: "orders", item: @item.id}%>
  </div>
</div>
